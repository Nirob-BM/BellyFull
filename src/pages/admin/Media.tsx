import { Card, CardContent } from '@/components/ui/card';

const Media = () => (
  <div className="space-y-6">
    <div>
      <h1 className="font-display text-3xl font-bold">Media Library</h1>
      <p className="text-muted-foreground">Upload and manage images</p>
    </div>
    <Card><CardContent className="py-12 text-center text-muted-foreground">Media library coming soon. Upload images via the menu manager for now.</CardContent></Card>
  </div>
);

export default Media;
